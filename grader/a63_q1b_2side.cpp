#include <iostream>
#include <vector>

using namespace std;

int n, w, k;
vector<int> l,r;

int main(){
    ios_base::sync_with_stdio(false); cin.tie(NULL);
    cin >> n >> w >> k;
    for(int i=1; i<=n; i++){
        cin >> l[i];
    }
    for(int i=1; i<=n; i++){
        cin >> r[i];
    }
}